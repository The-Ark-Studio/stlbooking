import type {ThemeConfig} from 'antd';
import Colors from '@constants/Colors';

const antdTheme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: Colors.primary500, // may be changed according to your design
  },
  components: {},
};

export default antdTheme;
